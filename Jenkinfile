pipeline {
    agent any

    stages {
        stage('code') {
            steps {
                echo 'code devolop'
            }
        }
              
                stage('build') {
            steps {
                echo 'build project'
            }
            
        }
          stage('test') {
            steps {
                echo 'test project'
            }
        }
    }
}
